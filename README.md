# sovol-completion-announcer
A simple python script that allows the user to execute commands on their device when their remote sovol sv06 ACE printer completes printing.
detects print cancelled as print complete
.service for user systemd autostart on graphical session start included
